{
  "build": {
    "buildpacks": [
      {
        "url": "heroku/python",
        "version": "3.12"
      }
    ]
  },
  "deploy": {
    "startCommand": "gunicorn asgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}
